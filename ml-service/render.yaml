services:
  - type: web
    name: smartshop-ml
    runtime: python
    buildCommand: pip install -r requirements.txt && python -c "import nltk; nltk.download('vader_lexicon'); nltk.download('punkt'); nltk.download('stopwords')"
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT